# Don't process these again in a tmux session. They are already set
# Need to keep this here for rvm
# source ~/.rvm/scripts/rvm

#export ruby=`which ruby`

# function _rvm_completion {
#   source ~/.rvm/scripts/zsh/Completion/_rvm
# }

# compdef _rvm_completion rvm

# For zsh-autosuggestions
# export ZSH_AUTOSUGGEST_USE_ASYNC=true
# export ZSH_AUTOSUGGEST_HIGHLIGHT_STYLE="fg=#000000,bg=white,bold,underline"
# source /usr/local/share/zsh-autosuggestions/zsh-autosuggestions.zsh

# fzf shell completion and key bindings
# [[ $- == *i* ]] && source "/usr/local/opt/fzf/shell/completion.zsh" 2> /dev/null
# [[ $- == *i* ]] && source "/usr/local/opt/fzf/shell/key-bindings.zsh" 2> /dev/null

# fpath=(/usr/local/share/zsh-completions $fpath)
# source /usr/local/share/zsh-syntax-highlighting/zsh-syntax-highlighting.zsh
